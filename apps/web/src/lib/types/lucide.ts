// Lucide icons are React ForwardRefExoticComponent which doesn't directly
// match Preact's ComponentType. This alias bridges the gap.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type LucideIcon = any;
